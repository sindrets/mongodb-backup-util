
export enum GlobalEvent {
    CONNECTED,					// Triggered once a connection to the database has been established
    EXIT,                       // When triggered: the program will close all connections and exit
}